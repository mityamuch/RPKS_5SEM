<Window x:Class="DXpress2022.Client.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FIrstMVVMProg.Client"
        mc:Ignorable="d"
        xmlns:viewModels="clr-namespace:FIrstMVVMProg.Client.ViewModels"
        d:DataContext="{d:DesignInstance viewModels:StartWindowViewModel}"
        Title="StartWindow" Height="450" Width="800" ResizeMode="NoResize"  WindowStartupLocation="CenterScreen" >
    <Window.DataContext>
        <viewModels:StartWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
       
        <Button 
            Grid.Column="1" 
            Grid.Row="0" 
            Name="btNext" 
            Content="КНОПКА" 
            Command="{Binding ExampleCommand}">
        </Button>
    </Grid>
</Window>
